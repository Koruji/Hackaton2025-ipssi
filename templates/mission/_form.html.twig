{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn rounded-pill mr-2" style="background-color: #ff863f; opacity: 0.85;">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
